const router = require('express').Router();
// const { addUser, loginUser, getMe} = require('../Controllers/users');

// router.route('/').post(addUser);
// router.route('/').post(loginUser);
// router.route('/:id').get(getMe);

module.exports = router;

